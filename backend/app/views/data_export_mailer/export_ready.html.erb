<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(135deg, #ff6b6b, #ffd93d, #6bcb77, #4d96ff, #a66dd4);
      padding: 30px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .header h1 {
      color: white;
      margin: 0;
      font-size: 24px;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
    }
    .content {
      background: #f9f9f9;
      padding: 30px;
      border-radius: 0 0 8px 8px;
    }
    .greeting {
      font-size: 18px;
      margin-bottom: 20px;
    }
    .message {
      margin-bottom: 25px;
    }
    .download-button {
      display: inline-block;
      background: #4d96ff;
      color: white;
      padding: 15px 30px;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      margin: 20px 0;
    }
    .download-button:hover {
      background: #3d7ad8;
    }
    .expiry-notice {
      background: #fff3cd;
      border: 1px solid #ffc107;
      padding: 15px;
      border-radius: 5px;
      margin: 20px 0;
    }
    .expiry-notice strong {
      color: #856404;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      font-size: 12px;
      color: #666;
    }
    .contents-list {
      background: white;
      padding: 15px;
      border-radius: 5px;
      margin: 15px 0;
    }
    .contents-list ul {
      margin: 10px 0;
      padding-left: 20px;
    }
    .contents-list li {
      margin: 5px 0;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= @app_name %></h1>
  </div>

  <div class="content">
    <p class="greeting">
      <%= t('data_export_mailer.export_ready.greeting', name: @user.display_name) %>
    </p>

    <p class="message">
      <%= t('data_export_mailer.export_ready.message') %>
    </p>

    <div class="contents-list">
      <strong><%= t('data_export_mailer.export_ready.contents_title') %></strong>
      <ul>
        <li><%= t('data_export_mailer.export_ready.contents.profile') %></li>
        <li><%= t('data_export_mailer.export_ready.contents.photos') %></li>
        <li><%= t('data_export_mailer.export_ready.contents.comments') %></li>
        <li><%= t('data_export_mailer.export_ready.contents.likes') %></li>
      </ul>
    </div>

    <p style="text-align: center;">
      <a href="<%= @download_url %>" class="download-button">
        <%= t('data_export_mailer.export_ready.download_button') %>
      </a>
    </p>

    <div class="expiry-notice">
      <strong><%= t('data_export_mailer.export_ready.expiry_notice') %></strong>
      <p><%= t('data_export_mailer.export_ready.expiry_message', time: l(@expires_at, format: :long)) %></p>
    </div>

    <p class="message">
      <%= t('data_export_mailer.export_ready.link_hint') %>
    </p>
    <p style="word-break: break-all; font-size: 12px; color: #666;">
      <%= @download_url %>
    </p>

    <div class="footer">
      <p><%= t('data_export_mailer.export_ready.footer') %></p>
    </div>
  </div>
</body>
</html>
